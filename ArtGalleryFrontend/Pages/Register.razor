@inject NavigationManager navigationManager
@page "/register"

<div class="form-container ">
    <div class="card shadow col-6 form-structure">
        <h2 class="form-title">Sign Up For Our Art </h2><h2 class="gold-colored-text form-title">Gallery</h2>

   <EditForm  Model="user" OnInvalidSubmit="DisplayName">
            <DataAnnotationsValidator/>
                <ValidationSummary/>
            <div class="form-group p-2">
                <label for="name">Name</label>

               <InputText class="form-control p-2" id="name" @bind-Value="user.UserName" aria-describedby="name" />
            </div>
        <div class="form-group p-2">
            <label for="name">Name</label>

            <InputText class="form-control p-2" id="name" @bind-Value="user.UserName" aria-describedby="name" />
        </div>
        <div class="form-group p-2">
            <label for="name">Name</label>

            <InputText class="form-control p-2" id="name" @bind-Value="user.UserName" aria-describedby="name" />
        </div>
        <div class="form-group p-2">
            <label for="name">Name</label>

            <InputText class="form-control p-2" id="name" @bind-Value="user.UserName" aria-describedby="name" />
        </div>
        <div class="form-group p-2">
            <label for="name">Name</label>

            <InputText class="form-control p-2" id="name" @bind-Value="user.UserName" aria-describedby="name" />
        </div>
        <div class=" button-container">
            <button class="form-button" @onclick="login">Register</button>
        </div>
   </EditForm>
    
</div>
</div>


@code{
    private UserDTO user = new UserDTO();
    public void DisplayName() {
        Console.WriteLine(user.UserName);
    }

    private void login()
    {
        navigationManager.NavigateTo("login"); 
    }
}